# Only needed for production deployment (Vercel).
# Set this to your deployed backend URL (from Render/Railway), e.g.:
# VITE_API_URL=https://techmart-backend.onrender.com
# Leave unset for local development -- it defaults to http://localhost:8000
VITE_API_URL=
